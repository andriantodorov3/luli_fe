import { mount } from '@vue/test-utils'
import Komponent from '@/components/DrawerHeader.vue'

describe('DrawerHeader.vue', () => {
    it('Sanity check', () => {
        mount(Komponent)
        return;
    })
})