import { mount } from '@vue/test-utils'
import Komponent from '@/views/Hdn.vue'

describe('Hdn.vue', () => {
    it('Sanity check', () => {
        const wrapper = mount(Komponent)
        return;
    })

})